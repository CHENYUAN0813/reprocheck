{
  "name": "reprocheck",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "node --watch-preserve-output --watch-path=server.mjs --watch-path=scan.mjs --watch-path=worker.mjs server.mjs",
    "prebuild": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "vite build --outDir dist/client && vite build --ssr worker.mjs --outDir dist/server",
    "preview": "vite preview",
    "test": "node scan.mjs --self-test && node server.mjs --self-test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "^0.2.0",
    "vite": "^8.2.2"
  }
}
